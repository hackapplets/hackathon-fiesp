defmodule Fiesp.Maps.ConnTest do
  # Case
  use ExUnit.Case

  test "should send a request to the Google Maps" do
    # req = Fiesp.Maps.Conn.directions("Mogi das Cruzes", "Av Paulista", mode: "walking")
    # IO.inspect req
  end
end
